# NPCGo → Wails Migration TODO

## 1. Prerequisites & Tooling

- [x] Install Wails CLI (`go install github.com/wailsapp/wails/v2/cmd/wails@latest`)
- [x] Verify Wails system dependencies are met (platform-specific: WebView2 on Windows, WebKit on Linux/macOS)
- [ ] Decide on a frontend framework for the UI (Svelte, React, Vue, or vanilla JS — Wails supports all via templates)
- [x] Install Node.js / npm (required by Wails for frontend build tooling)

---

## 2. Project Restructure

- [ ] Run `wails init` in a scratch directory to study what a standard Wails project layout looks like, then plan how to merge it with the existing repo structure
- [ ] Move or rename the current `cmd/` entrypoint — Wails expects its own `main.go` at the project root (or a dedicated location) that calls `wails.Run()`
- [ ] Create a `frontend/` directory at the repo root for all UI source files (HTML/CSS/JS or your chosen framework)
- [ ] Decide whether to keep `cmd/` for a separate CLI build target or remove it entirely now that the app is desktop-only
- [ ] Update `go.mod` module path if needed once the project root changes

---

## 3. Remove / Replace Web Server Code

- [ ] Remove `github.com/gin-gonic/gin` from `go.mod` — Gin's HTTP server is no longer needed since Wails handles the bridge between Go and the frontend
- [ ] Audit `internal/` for any code that is tightly coupled to Gin request/response handlers (routes, middleware, HTTP context usage)
- [ ] Refactor that handler logic into plain Go methods on a struct that will become the Wails **App** struct (the bound backend)
- [ ] Remove any HTTP-specific concepts (status codes, request parsing, JSON response writers) from business logic — replace with direct return values

---

## 4. Create the Wails App Struct

- [ ] Create an `app.go` (or equivalent) at the project root defining a struct (e.g. `App`) that holds your application's dependencies (DB connection, config, generator instances, etc.)
- [ ] Implement `startup`, `shutdown`, and `domReady` lifecycle methods on the `App` struct as needed by Wails
- [ ] Move NPC generation logic references into methods on `App` so Wails can bind and expose them to the frontend via `wails.Bind(&app)`

---

## 5. Database Handling

- [ ] Decide whether to keep SQLite (good for a local desktop app) or drop PostgreSQL support — PostgreSQL is unlikely to be useful for a standalone desktop tool
- [ ] Remove `github.com/jackc/pgx/v4` and related `pgconn`, `pgtype`, `pgproto3` packages from `go.mod` if PostgreSQL is being dropped
- [ ] Ensure the SQLite database file path resolves correctly for a packaged desktop app (use Wails' runtime to get the app data directory rather than a hardcoded relative path)
- [ ] Update `db/` migrations and seed scripts to only target SQLite if PostgreSQL is removed

---

## 6. Data / Asset Embedding

- [ ] Audit `data/raw/` and `configs/` — any files the app needs at runtime must either be embedded via `//go:embed` or bundled by Wails into the final binary/app package
- [ ] Add `//go:embed` directives for data files that are read at runtime (word lists, grammar rule sets, YAML configs, etc.)
- [ ] Confirm Wails' `embed.FS` integration for the frontend assets (`frontend/dist`) is set up correctly in `main.go`

---

## 7. Build System

- [x] Replace or significantly update `scripts/build.sh` — the current cross-platform build script targets a CLI binary; it needs to be replaced with `wails build` commands
- [x] Add `wails dev` as the local development workflow (hot-reloads both Go backend and frontend)
- [x] Add platform-specific `wails build` targets for Linux, Windows, and macOS to the build script
- [ ] Remove or archive the old `build.sh` cross-compilation logic (`GOOS=windows`, `GOOS=linux`, etc.) since Wails handles packaging differently (it must be built on the target OS or via cross-compile with extra setup)
- [ ] Add a `wails.json` config file (auto-generated by `wails init`, but needs to be populated with app name, version, bundle ID, etc.)
- [x] Add error checking into build script

---

## 8. Frontend UI (Net New Work)

- [ ] Design and build the frontend UI that calls the bound Go methods — at minimum a "Generate NPC" button and a display area for the output
- [ ] Wire up the Wails JS runtime bindings (`window.go.<MethodName>()`) to UI events
- [ ] Handle async responses from Go methods in the frontend (Wails bindings return Promises)
- [ ] Add any loading/error states to the UI for long-running generation calls

---

## 9. README & Docs Update

- [ ] Update `README.md` install instructions — no more downloading a zip and moving a binary; the app will be a `.exe`, `.app`, or `.AppImage`
- [ ] Update the directory overview section to reflect the new project structure
- [ ] Remove references to `npcgen help` CLI usage if the CLI entrypoint is being removed
